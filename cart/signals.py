# from django.template.defaultfilters import slugify
# from django.db.models import signals
# from django.dispatch import receiver

# def atualizar_total_item(signal, instance, sender, **kwargs):
#     instance.total_item_carrinho = (instance.produto_item.preco_unitario *
#       instance.quantidade)

# signals.pre_save.connect(atualizar_total_item, sender=ItemDoCarrinho)
